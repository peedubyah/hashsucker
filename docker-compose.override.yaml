# Local operator overrides (optional, UNTRACKED semantics documented here).
#
# This file intentionally sets nothing active: there are currently no
# operator override knobs that belong here. The background durability
# observer (BACKGROUND_DURABILITY_MODE) was retired — setting it has no
# effect. Kept as a documented placeholder so local experiments have a
# home that never collides with the base compose file.
services: {}
